{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "TSCompileAll",
            "type": "shell",
            "command": "./node_modules/.bin/tsc --watch --noEmit --project ./tsconfig.json",
            "problemMatcher": ["$tsc-watch"]
        }
    ]
}
